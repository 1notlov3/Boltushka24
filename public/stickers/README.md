# Stickers

`manifest.json` is the stable registry for first-party sticker packs. UI can load this file directly and render each entry by `type`.

Current types:

- `emoji`: lightweight built-in stickers that need no network fetch.
- `lottie`: reserved for animated `.json` stickers in this directory.
- `png`: reserved for raster stickers in this directory.
